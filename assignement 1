//write a c program to print smallest number among n numbers
#include<stdio.h>
void main()
{
	int i,ele,n;
	for(i=0;i<=n;i++)
	{
	scanf("%d",&ele);
	if(ele>i)
	i=ele;
    }
    printf("\n the smallest ele =%d",i);
}
